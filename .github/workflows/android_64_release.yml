# The 64-bit version of this action (arm64 only)
name: Fortis Control Android 64-bit Release

on:
  push:
    branches:
      - 'master'
      - 'Stable*'
      - 'main'
    tags:
      - 'v*'
  pull_request:
    branches:
    - '*'

defaults:
  run:
    shell: bash

env:
  SOURCE_DIR:   ${{ github.workspace }}
  QT_VERSION:   5.15.2
  BUILD_TYPE:   ${{ fromJSON('["DailyBuild", "StableBuild"]')[ github.ref_type == 'tag' || contains(github.ref, 'Stable_' ) ] }}


jobs:
  build:
    runs-on:  ubuntu-latest

    strategy:
      matrix:
       include:
         - architecture: 64bits
           eabi: arm64-v8a
           ARTIFACT: QGroundControl64.apk

    steps:

      ##### OLD STEPS INSTALL JAVA 11 AND NDK 21E #####
      # - name: Free disk space
      #   run: |
      #     sudo rm -rf /usr/share/dotnet
      #     sudo rm -rf /opt/ghc
      #     sudo rm -rf "/usr/local/share/boost"
      #     sudo rm -rf "$AGENT_TOOLSDIRECTORY"

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # - uses: actions/checkout@v3
      # - uses: nttld/setup-ndk@v1
      #   id: setup-ndk
      #   with:
      #     ndk-version: r21e
      #     add-to-path: false
      
      - name: Install Android SDK
        uses: android-actions/setup-android@v2
        with:
          api-level: 34                  # ใช้ platform android-34
          build-tools: 34.0.0            # ใช้ build-tools 34.0.0
          ndk: 25.2.9519653              # Qt ชอบ NDK r25
          cmake: 3.22.1
      
      - name: Ensure NDK 25.2 installed
        run: |
          sdkmanager "ndk;25.2.9519653"
          sdkmanager --list | grep ndk

      - name: Export NDK path
        run: echo "ANDROID_NDK_ROOT=$ANDROID_HOME/ndk/25.2.9519653" >> $GITHUB_ENV

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Get all tags for correct version determination
        working-directory:  ${{ github.workspace }}
        run: |
          git fetch --all --tags -f

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version:      ${{ env.QT_VERSION }}
          host:         linux
          target:       android
          dir:          ${{ runner.temp }}
          modules:      qtcharts
          setup-python: true

      - name: Install ccache
        run:  sudo apt-get install ccache

      - name: Prepare ccache timestamp
        id: ccache_cache_timestamp
        shell: cmake -P {0}
        run: |
          string(TIMESTAMP current_date "%Y-%m-%d-%H;%M;%S" UTC)
          message("::set-output name=timestamp::${current_date}")
      - name: ccache cache files
        uses: actions/cache@v4
        with:
          path:         ~/.ccache
          key:          ${{ runner.os }}-ccache-${{steps.ccache_cache_timestamp.outputs.timestamp}}
          restore-keys: ${{ runner.os }}-ccache-

      - name: Setup ccache
        run: |
            mkdir -p ~/.ccache
            echo "base_dir = ${GITHUB_WORKSPACE}" > ~/.ccache/ccache.conf
            echo "compression = true" >> ~/.ccache/ccache.conf
            echo "compression_level = 5" >> ~/.ccache/ccache.conf
            ccache -s
            ccache -z
      - name: Create build directory
        run:  mkdir ${{ runner.temp }}/shadow_build_dir

      - name:               Install gstreamer
        working-directory:  ${{ github.workspace }}
        run: |
            wget --quiet https://gstreamer.freedesktop.org/data/pkg/android/1.18.6/gstreamer-1.0-android-universal-1.18.6.tar.xz
            mkdir gstreamer-1.0-android-universal-1.18.6
            tar xf gstreamer-1.0-android-universal-1.18.6.tar.xz -C gstreamer-1.0-android-universal-1.18.6
      # This will set GIT_BRANCH_NAME environment variable
      - name: Git branch name
        id:   git-branch-name
        uses: EthanSK/git-branch-name-action@v1

      - name: Update android manifest
        run: |
          if [ $GIT_BRANCH_NAME != "Stable*" ]; then
            ${SOURCE_DIR}/tools/update_android_manifest_package.sh ${GIT_BRANCH_NAME}
          fi
      - name: Ensure android-build libs dirs
        run: |
          mkdir -p "${{ runner.temp }}/shadow_build_dir/android-build/libs/arm64-v8a"
          ls -ld "${{ runner.temp }}/shadow_build_dir/android-build/libs" || true

      - name: Build
        working-directory: ${{ runner.temp }}/shadow_build_dir
        # env:
          # ANDROID_NDK: ${{ steps.setup-ndk.outputs.ndk-path }}
          # ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
          # ANDROID_NDK_LATEST_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
          # ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}
        run:  |
            qmake -r ${SOURCE_DIR}/qgroundcontrol.pro -spec android-clang CONFIG+=${BUILD_TYPE} CONFIG+=installer ANDROID_ABIS="${{ matrix.eabi }}"
            make -j2 apk
            mkdir ${{ runner.temp }}/shadow_build_dir/package
            mv ${{ runner.temp }}/shadow_build_dir/android-build/build/outputs/apk/debug/android-build-debug.apk ${{ runner.temp }}/shadow_build_dir/package/${{ matrix.ARTIFACT }}
      - name: ccache post-run
        run:  ccache -s

      - name: Save artifact
        uses: actions/upload-artifact@master
        with:
          name: ${{ matrix.ARTIFACT }}
          path: ${{ runner.temp }}/shadow_build_dir/package/${{ matrix.ARTIFACT }}

      # - name: Upload build to S3 Bucket
      #   if:                 github.event_name == 'push'
      #   working-directory:  ${{ runner.temp }}/shadow_build_dir/package
      #   run: |
      #         aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
      #         aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #         aws s3 cp ${{ matrix.ARTIFACT }} s3://qgroundcontrol/builds/${GIT_BRANCH_NAME}/${{ matrix.ARTIFACT }} --region us-west-2 --acl public-read

      # - name: Upload tagged stable build to S3 latest Bucket
      #   if:                 github.event_name == 'push' && github.ref_type == 'tag'
      #   working-directory:  ${{ runner.temp }}/shadow_build_dir/package
      #   run: |
      #         aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
      #         aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #         aws s3 cp ${{ matrix.ARTIFACT }} s3://qgroundcontrol/latest/${{ matrix.ARTIFACT }} --region us-west-2 --acl public-read